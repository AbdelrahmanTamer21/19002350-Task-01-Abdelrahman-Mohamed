services:
  app:
    build: .
    container_name: 55-22769_container
    ports:
      - "22769:8080"
    environment:
      - USER_NAME=Compose_Abdelrahman_Mohamed
      - ID=Compose_55_22769
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
